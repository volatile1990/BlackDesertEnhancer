{
  "name": "black-desert-enhancer-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "node --test scripts/*.test.mjs && vitest run",
    "test:watch": "vitest",
    "check": "npm run test && npm run build",
    "snapshot": "node scripts/update-market-snapshot.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
